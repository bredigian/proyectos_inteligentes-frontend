export type TContact = {
  name: string;
  email: string;
  phone: string;
  message: string;
};

export type TQuote = {
  name: string;
  email: string;
  phone: string;
  date: Date;
  message: string;
  services: string[];
};
